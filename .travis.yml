language: clojure

before_script:
  - "psql -c 'create database test;' -U postgres"
  - "psql -c \"create user database UNENCRYPTED PASSWORD 'database'\" test -U postgres"

lein: lein2

postgres:
  adapter: postgresql
  database: test
  username: postgres